\documentclass[a5paper, landscape, 11pt, twoside]{scrartcl}

\usepackage{graphicx}
\usepackage{fontspec}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{siunitx}
\usepackage{float}
\usepackage[export]{adjustbox}

% font change
\usepackage[default]{raleway}

% Cuisine settings
\usepackage[nonumber]{cuisine}
% Widths: total, step number, step number sep, ingredient, ingredient sep, method
\RecipeWidths{1.24\textwidth}{0.25\linewidth}{1.2em}{9em}{2.5em}{2.6em}
\renewcommand*{\recipetitlefont}{\large\bfseries\sffamily}
\renewcommand*{\recipestepnumberfont}{\tiny\bfseries\sffamily}



\usepackage{tikz}

% Define a command to add a circle around the step number
\newcommand{\AddCircleAroundStepNumber}{%
	\tikz[baseline=(char.base)]{
		\node[draw, circle, inner sep=0.11em, line width=0.12em ] (char) {\arabic{st@pnumber}};
	}%
}

% Override the DisplayStep macro to include the circle around the step number
\makeatletter
\renewcommand{\Displ@ySt@p}{%
	\stepcounter{st@pnumber}%
	\makebox[\R@cipeWidth]{%
		\makebox[\St@pNumberWidth][l]{
			%\recipestepnumberfont\AddCircleAroundStepNumber
			\recipestepnumberfont\raisebox{0.3em}{\AddCircleAroundStepNumber}%
		}%
		\usebox{\st@pingrbox}%
		\usebox{\st@pmethodbox}%
	}%
	\savebox{\st@pingrbox}[\R@cipeIQUWidth]{}%
}
\makeatother

% Adjust margins for all sides individually
\geometry{
	left=1.25cm,
	right=1.25cm,
	top=1cm,
	bottom=1cm,
}

\title{PP's Recipe Collection}
\author{PP}


\begin{document}
\begin{small}
	